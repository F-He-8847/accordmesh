import type { LocaleCode } from "./locale-types";

export const officialLocale: LocaleCode = "en";
export const registeredLocales: Array<{ code: LocaleCode; label: string }> = [{ code: "en", label: "English" }];
